//Player factory function to return player objects
const player = (playerNumber, playerType, playerTurn) => {
    return {playerNumber, playerType, playerTurn};
}

//Gameboard module to return gameboard object
const gameBoard = (() => {

})();